Search for IFS Cloud ERP tools. Use to discover available operations.

- `query="inventory"` → search for inventory-related tools
- `query="select:tool_name"` → load specific tool's full schema

Returns tool names and descriptions. Use select: to load before calling.
