Spawn a subagent for isolated task execution.

- `subagent_type="Explore"` → fast data gathering
- `subagent_type="Plan"` → design implementation approach
- `subagent_type="general-purpose"` → full agent capabilities

Subagent has fresh context - include all needed information in prompt.
